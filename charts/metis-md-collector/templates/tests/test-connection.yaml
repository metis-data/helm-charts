apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metis-md-collector.fullname" . }}-test-connection"
  labels:
    {{- include "metis-md-collector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metis-md-collector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
